#include <iostream>
#include <fstream>
#include <map>
using namespace std;
int n;
map<string, int>
vector<vector<int> > adj();
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    cin >> n;
    for (int i = 1; i < n; i++) {
        cin >> str.first >> str.second;
        par[str.first] = str.second;
    }
    sreturn 0;
}
